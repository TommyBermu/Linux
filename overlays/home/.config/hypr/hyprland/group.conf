group {
    col.border_active = $groupedWindowBorderColour
    col.border_inactive = $inactiveWindowBorderColour
    col.border_locked_active = $groupedLockedWindowBorderColour
    col.border_locked_inactive = $inactiveWindowBorderColour

    groupbar {
        enabled = false

        #this config doesn't work, to make it work you need to set the groupbar to "enabled = true"
        font_family = JetBrains Mono NF
        font_size = 15
        gradients = true
        gradient_round_only_edges = false
        gradient_rounding = 5
        height = 25
        indicator_height = 0
        gaps_in = 3
        gaps_out = 3

        text_color = rgb($onPrimary)
        col.active = rgba($primaryd4)
        col.inactive = rgba($outlined4)
        col.locked_active = rgba($primaryd4)
        col.locked_inactive = rgba($secondaryd4)
    }
}
